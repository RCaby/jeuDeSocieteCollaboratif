package src.old;

import javax.swing.JButton;
import javax.swing.JComboBox;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import java.awt.BorderLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class WelcomeScreen extends JPanel {
    /*
     * private static final long serialVersionUID = -2762873439941411862L; MainFrame
     * frame; public static final String WELCOME_SCREEN = "WELCOME_SCREEN";
     * JComboBox<Integer> nbPlayerComboBox;
     * 
     * public WelcomeScreen(MainFrame frame) { super(); this.frame = frame;
     * 
     * JPanel mainPanel = new JPanel(); add(mainPanel); mainPanel.setLayout(new
     * BorderLayout());
     * 
     * JLabel displayNbPlayers = new JLabel("Number of player : "); JButton
     * buttonValidate = new JButton("Start !"); buttonValidate.addActionListener(new
     * StartButtonListener()); nbPlayerComboBox = new JComboBox<>(); for (int nb =
     * 3; nb < 13; nb++) { nbPlayerComboBox.addItem(nb); } JPanel panelPanel = new
     * JPanel(); panelPanel.add(displayNbPlayers); panelPanel.add(nbPlayerComboBox);
     * panelPanel.add(buttonValidate); mainPanel.add(panelPanel);
     * 
     * }
     * 
     * private class StartButtonListener implements ActionListener {
     * 
     * @Override public void actionPerformed(ActionEvent e) {
     * frame.setNbPlayers((int) nbPlayerComboBox.getSelectedItem()); }
     * 
     * }
     */
}
