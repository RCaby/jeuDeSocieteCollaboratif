package src.old;

import java.io.Serializable;
import java.util.ArrayList;
import java.util.List;

import src.back.cards.Card;

public class Player implements Serializable {
    /*
     * private static final long serialVersionUID = 2066759979592279694L; private
     * List<Card> cardList; PlayerState statePlayer; boolean isChief; int
     * roundSickness;
     * 
     * public Player() { cardList = new ArrayList<>(); statePlayer =
     * PlayerState.HEALTHY; }
     * 
     * public void receiveCard(Card card) { cardList.add(card); }
     * 
     * public void discardCard(Card card) { cardList.remove(card); }
     * 
     * public List<Card> getCardList() { return cardList; }
     * 
     * public PlayerState getStatePlayer() { return statePlayer; }
     * 
     * public void setPlayerSick(int round) { statePlayer = PlayerState.SICK;
     * roundSickness = round; }
     * 
     * public void curePlayer() { roundSickness = -1; statePlayer =
     * PlayerState.HEALTHY; }
     * 
     * public int getRoundSickness() { return roundSickness; }
     * 
     * public boolean isPlayerChief() { return isChief; }
     * 
     * public void setPlayerChief(boolean playerIsChief) { isChief = playerIsChief;
     * }
     * 
     * public void killPlayer() { statePlayer = PlayerState.DEAD; }
     * 
     * public void resurrectPlayer() { statePlayer = PlayerState.HEALTHY; }
     * 
     * public boolean isPlayerDead() { return statePlayer == PlayerState.DEAD; }
     * 
     * public boolean isPlayerSick() { return statePlayer == PlayerState.SICK; }
     * 
     * public boolean isPlayerHealthy() { return statePlayer == PlayerState.HEALTHY;
     * }
     * 
     * public String toString() { StringBuilder string = new StringBuilder(); for
     * (Card card : cardList) { string.append(card + ", "); } return
     * "Joueur, possède " + string; }
     */
}
