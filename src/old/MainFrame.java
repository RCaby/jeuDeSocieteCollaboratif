package old;

import javax.swing.JButton;
import javax.swing.JComboBox;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import java.awt.BorderLayout;
import java.awt.CardLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.util.List;

// import src.front.EndScreen;
// import src.back.MainBoard;

public class MainFrame extends JFrame {
    /*
     * private static final long serialVersionUID = 8330640038052674556L;
     * 
     * int nbPlayer; JPanel mainPanel; CardLayout cardLayout; MainScreenGame
     * mainScreenGame; WelcomeScreen welcomeScreen; EndScreen endScreen; MainBoard
     * board;
     * 
     * public MainFrame() { super("Galèrapagos");
     * 
     * welcomeScreen = new WelcomeScreen(this); mainScreenGame = new
     * MainScreenGame(this); endScreen = new EndScreen(); mainPanel = new JPanel();
     * add(mainPanel); cardLayout = new CardLayout();
     * mainPanel.setLayout(cardLayout); mainPanel.add(welcomeScreen,
     * WelcomeScreen.WELCOME_SCREEN); mainPanel.add(mainScreenGame,
     * MainScreenGame.MAIN_SCREEN_GAME); mainPanel.add(endScreen,
     * EndScreen.END_SCREEN); }
     * 
     * public void setNbPlayers(int nb) { nbPlayer = nb;
     * mainScreenGame.setNbPlayers(nb); cardLayout.show(mainPanel,
     * MainScreenGame.MAIN_SCREEN_GAME); initiateBoard();
     * 
     * }
     * 
     * public void initiateBoard() { board = new MainBoard(nbPlayer); int food =
     * board.getFood(); int water = board.getWater(); int weather =
     * board.getWeather(); mainScreenGame.initializeInterface(food, water, weather);
     * mainScreenGame.setBoard(board); mainScreenGame.setOtherPlayers(); }
     * 
     * public void endGame(List<String> playersAlive) {
     * endScreen.alivePlayers(playersAlive); cardLayout.show(mainPanel,
     * EndScreen.END_SCREEN); }
     */
}
